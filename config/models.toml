# Mizan v1.0 model registry.
#
# `id` is the Vercel AI Gateway model slug (<provider>/<model>). Verify slugs
# against the gateway's catalogue (GET /v1/models) before a run — vendors
# rotate model names frequently.

[[models]]
id = "openai/gpt-5.5"
provider = "openai"
display_name = "GPT-5.5"

[[models]]
id = "anthropic/claude-sonnet-5"
provider = "anthropic"
display_name = "Claude Sonnet 5"

[[models]]
id = "google/gemini-3.1-pro-preview"
provider = "google"
display_name = "Gemini 3.1 Pro (preview)"

[[models]]
id = "xai/grok-4.3"
provider = "xai"
display_name = "Grok 4.3"

[[models]]
id = "meta/llama-4-maverick"
provider = "meta"
display_name = "Llama 4 Maverick"
