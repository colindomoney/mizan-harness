# Vercel AI Gateway API key (one key, all models).
# Create one at: https://vercel.com/docs/ai-gateway → your team → AI Gateway → API keys.
# Copy this file to .env — the real .env is gitignored and must never be committed.
AI_GATEWAY_API_KEY=
